export const BASE_URL = 'https://radiant-earth-37235.herokuapp.com/';

export enum UrlParams {
  signin = 'signin',
  signup = 'signup',
  users = 'users',
  boards = 'boards',
  columns = 'columns',
  tasks = 'tasks',
}

export enum QueryType {
  get = 'GET',
  put = 'PUT',
  post = 'POST',
  delete = 'DELETE',
}

export const TOKEN = 'token';
